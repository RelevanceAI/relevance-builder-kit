# Workforce Edges

> **STUB.** Pending net-new content in Phase 3 of the build-kit restructure.

## Planned Coverage

- Edge types: `forced-handover` vs `tool-call` (deeper than `workforce-patterns.md` overview)
- `params_schema` design patterns (worked examples)
- The `additionalProperties: true` rule and the `_subagent_params` runtime injection
- Threading-behavior decision tree (`always-create-new` vs `always-same` vs `auto`)
- Dispatch-pattern decision (forced-handover for deterministic routing vs tool-call for agent-driven delegation)
- Worked examples for each pattern
- MCP research source: `relevance_get_workforce` introspection of real workforces

## Until Then

- See `workforce-patterns.md` for the mental model and headline rules
- See `.claude/rules/PLATFORM_MECHANICS.md` § "Workforce Architecture" § "Edge Types"
- See `.claude/rules/BUILD_PRACTICES.md` § "Workforce / Orchestrator Patterns"
